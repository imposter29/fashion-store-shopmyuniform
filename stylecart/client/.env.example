# Client environment (Vite). Only needed for the split production deploy.
#
# Absolute base URL of the Render API, INCLUDING the /api suffix.
# In development leave this unset — Vite proxies /api to the local server.
# VITE_API_URL=https://stylecart-api.onrender.com/api
